# Python-Log-Parser-for-Security-Event-Analysis

# Security Log Parser (Python)

A lightweight Python tool that parses Linux authentication logs (`/var/log/auth.log`) and identifies failed SSH login attempts. This script is designed as a simple, beginner‑friendly security utility that demonstrates log analysis, regex matching, and basic threat detection techniques.

---

## 🔍 Features

- Extracts failed SSH login attempts  
- Counts attempts per IP address  
- Uses a simple regex pattern for clarity  
- Works on any Linux system with an auth log  
- Beginner‑friendly, clean, and easy to extend  

---

## 📁 Repository Structure

